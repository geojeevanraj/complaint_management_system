# Test environment variables for CI/CD
# This file sets environment variables that allow tests to run without database connections

DB_SERVER=localhost
DB_NAME=complaint_system_test
DB_USER=test_user
DB_PASSWORD=test_password
DB_PORT=3306
DB_DRIVER={MySQL}

# Skip database driver check in CI environments
SKIP_DB_DRIVER_CHECK=true

# Test-specific settings
TESTING=true
